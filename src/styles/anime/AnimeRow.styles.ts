import { StyleSheet } from 'react-native';

import { spacing } from '@/theme';

export const animeRowStyles = StyleSheet.create({
    container: {
        marginBottom: spacing.xxxl,
    },

    listContent: {
        paddingTop: spacing.sm,
    },
});