import { spacing } from "@/theme";
import { StyleSheet } from "react-native";

export const recommendationRowStyles = StyleSheet.create({
  content: {
    paddingRight: spacing.lg,
  },

  itemSeparator: {
    width: spacing.md,
  },
});
